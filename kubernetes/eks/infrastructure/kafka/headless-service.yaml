apiVersion: v1
kind: Service
metadata:
  name: zord-kafka-headless
spec:
  clusterIP: None
  selector:
    app.kubernetes.io/name: zord-kafka
  ports:
    - name: kafka
      port: 9092
      targetPort: 9092
    - name: controller
      port: 9093
      targetPort: 9093
