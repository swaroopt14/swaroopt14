apiVersion: v1
kind: Service
metadata:
  name: fluentd-http
  namespace: logging
  labels:
    app: fluentd
spec:
  selector:
    app: fluentd
  ports:
    - name: http
      port: 9880
      targetPort: 9880
      protocol: TCP
