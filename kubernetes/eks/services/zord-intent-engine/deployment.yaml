apiVersion: apps/v1
kind: Deployment
metadata:
  name: zord-intent-engine
spec:
  replicas: 2
  minReadySeconds: 10
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: zord-intent-engine
  template:
    metadata:
      labels:
        app.kubernetes.io/name: zord-intent-engine
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8083"
        prometheus.io/path: "/metrics"
    spec:
      serviceAccountName: zord-aws-access
      terminationGracePeriodSeconds: 45
      affinity:
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            - labelSelector:
                matchLabels:
                  app.kubernetes.io/name: zord-intent-engine
              topologyKey: kubernetes.io/hostname
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: topology.kubernetes.io/zone
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app.kubernetes.io/name: zord-intent-engine
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: DoNotSchedule
          labelSelector:
            matchLabels:
              app.kubernetes.io/name: zord-intent-engine
      containers:
        - name: zord-intent-engine
          image: 522189039032.dkr.ecr.ap-south-1.amazonaws.com/zord/zord-intent-engine:v6
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8083
              name: http
          env:
            - name: KAFKA_BROKERS
              valueFrom:
                configMapKeyRef:
                  name: zord-aws-config
                  key: KAFKA_BROKERS
            - name: KAFKA_TOPIC
              value: payments.ledger.events.v1
            - name: KAFKA_TOPIC_PII_TOKENIZE_REQUEST
              value: pii.tokenize.request
            - name: KAFKA_TOPIC_PII_TOKENIZE_RESULT
              value: pii.tokenize.result
            - name: ZORD_PII_ENCLAVE_URL
              value: http://zord-token-enclave:8087
            - name: ENCLAVE_INTERNAL_TOKEN
              valueFrom:
                secretKeyRef:
                  name: zord-app-secrets
                  key: ENCLAVE_INTERNAL_TOKEN
            - name: DB_HOST
              valueFrom:
                configMapKeyRef:
                  name: zord-aws-config
                  key: DB_HOST
            - name: DB_PORT
              valueFrom:
                configMapKeyRef:
                  name: zord-aws-config
                  key: DB_PORT
            - name: DB_USER
              value: intent_user
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: zord-app-secrets
                  key: INTENT_DB_PASSWORD
            - name: DB_NAME
              value: zord_intent_engine_db
            - name: DB_SSLMODE
              valueFrom:
                configMapKeyRef:
                  name: zord-aws-config
                  key: DB_SSLMODE
            - name: AWS_REGION
              valueFrom:
                configMapKeyRef:
                  name: zord-aws-config
                  key: AWS_REGION
            - name: AWS_EC2_METADATA_DISABLED
              valueFrom:
                configMapKeyRef:
                  name: zord-aws-config
                  key: AWS_EC2_METADATA_DISABLED
            - name: S3_BUCKET
              valueFrom:
                secretKeyRef:
                  name: zord-app-secrets
                  key: INTENT_S3_BUCKET
            - name: ZORD_VAULT_KEY
              valueFrom:
                secretKeyRef:
                  name: zord-app-secrets
                  key: ZORD_VAULT_KEY
            - name: RELAY_AUTH_TOKEN
              valueFrom:
                secretKeyRef:
                  name: zord-app-secrets
                  key: RELAY_SERVICES_0_AUTH_TOKEN
            - name: WORKER_POOL_SIZE
              value: "64"
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              valueFrom:
                configMapKeyRef:
                  name: zord-aws-config
                  key: OTEL_EXPORTER_OTLP_ENDPOINT
            - name: OTEL_SERVICE_NAME
              value: zord-intent-engine
          readinessProbe:
            httpGet:
              path: /health
              port: 8083
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8083
            initialDelaySeconds: 30
            periodSeconds: 20
          resources:
            requests:
              cpu: 200m
              memory: 384Mi
            limits:
              cpu: 750m
              memory: 768Mi
